# Test-Only 排班與回診工作臺規格

**狀態：合成測試設計（本機或已授權的到期 Hosting preview）。** 本文件把工作臺
與患者端的功能邊界寫成可實作、可測試的規格；它不是正式回診醫囑、公開掛號服務
或正式作業規範。D-006 已核准但未實作；所有正式值與啟用仍受其他對應 gate、
Stage 2 change/deployment review 與 D-006 實作證據約束。

## 目標與邊界

工作臺應讓授權人員配置下列內容：

1. 每週的開放星期與一日內一或多段可預約時間。
2. 指定日期的加開時段與休息／休診日。
3. 回診需求的人工確認與回診目標日期。
4. 患者端只看到本人可預約的回診選項，並建立或取消自己的回診預約。

本階段只會做雙重開關的 loopback、合成資料模型與介面，以及決策登錄另行授權的
到期 static Hosting preview。不得使用真實病患、病歷、聯絡方式、職員身分、
Google Calendar、Firebase backend、LINE、Meta 或 NAS。`apps/api` 仍為唯一正式
寫入邊界；未來 Android/iOS 也只能呼叫該 API。

## 排班資料模型

所有時間以 `Asia/Taipei` 顯示、以 UTC 保存。每筆規則都有 `scheduleVersion`、
建立／修改 actor、時間與原因代碼的稽核資料；原因不得蒐集自由文字病情。

| 模組 | 最小欄位 | 驗證與不變條件 |
| --- | --- | --- |
| 每週開放規則 | `weekday`、`startLocalTime`、`endLocalTime`、`resourceId` | 星期為 0–6；開始小於結束；同一資源同日區間不得重疊。 |
| 日期例外 | `date`、`kind` (`closed` / `extra_open`)、`intervals`、`reasonCode` | `closed` 不帶時段；`extra_open` 至少一段有效時段；不可覆蓋為另一種例外。 |
| 時段產生 | 規則版號、服務時長、容量、預約範圍 | 由 API／領域規則產生；前端與 Calendar 均不得把「空檔」當作權威。 |
| 回診決定 | `followUpStatus`、`dueDate`、`decidedBy`、`decidedAt` | 僅授權臨床／營運角色可寫；`required` 才可有 `dueDate`。 |

### 日期例外的測試用優先順序

1. `closed`：該資源該日不提供任何時段。
2. `extra_open`：在每週規則外增加明確區間；與每週規則重疊時必須先拒絕或由
   授權人員改為非重疊區間，不能自動合併。
3. 無例外：採用每週開放規則。

這是合成測試的明確、可驗證規則，並非診所正式排班決策；正式優先順序與核准
流程由 D-004 填定。

目前合成 preview 依 2026-07-27 業主建置方向顯示週三至週五
12:00–20:00、週六 10:00–18:00，週日至週二休診；任何 `20:30` seed 都已過時。
但 2026-07-28 重查
[診所官方預約頁](https://beauessence.com.tw/reservations/)時，官網仍發布週一至週五
11:00–20:00、週六 11:00–16:00。業主隨後選定合成 preview 的週三至週五
12:00–20:00、週六 10:00–18:00 為正式營業時間，因此 repository 不需改動，官網
內容需另行更正。2026-07-28 最終確認止鼾／醫美實際療程時長到診後才形成，不在
服務目錄寫死分鐘，也不因多選服務自動相加；正式 operational slot block、
capacity、resource、buffer／overrun 與 blackout policy 仍須由營運負責人補齊。

## 回診安全模型

系統的狀態只有 `unknown`、`not_required`、`required`：

- `unknown` 是預設值，患者端不可將其視為不需回診。
- `not_required` 與 `required` 必須是醫師或診所授權人員的明確決定，留下 actor、
  時間、前後狀態與規則版號。
- 系統不得依服務類型、過往預約、時間間隔或任何演算法自行推論「是否需要回診」。
- `required` 可設定一個回診目標日期；它是排程目標，不是診斷、醫囑全文或病歷。
- 取消、改期與逾期提醒仍須 D-005；個案管理師指派仍須 D-007。D-006 已核准，
  但相關正式功能仍須先完成 Stage 2 的 server-side 身分、權限與稽核實作。

## 本機測試介面

工作臺在 `TEST_ONLY_WEB_ENABLED=true` 條件下於本機提供，也可發布到已記錄的
Hosting preview channel，環境徽章會標示目前是哪一種。

**目前有效範圍（2026-07-27）**：預約表單可收姓名、電話、生日（月／日必填、
年份選填）、身分證／新式居留證或護照、健保卡攜帶意向、短患者備註、核准的
門診／來源標籤，以及符合條件時選填的介紹人姓名。LineID 與性別不收集。這些值
只存在該瀏覽器的 `localStorage`；畫面與清單遮罩身分文件，明示為合成測試的初診
資料列印才可顯示完整測試值。

**歷史輸入（2026-07-21，已被上段取代）**：當時預約表單只記錄姓名、電話、完整
生日與身分證字號。此句保留作變更歷史，不得再用作目前欄位 allowlist 或 identity
key 規格。工作臺其餘輸入（排班時間、日期例外、帳號標籤、公告）仍不得出現病情、
病歷或薪資金額。

資料流（全部在瀏覽器內完成，沒有 API 呼叫）：

```text
患者端 -> store.js 讀取回診狀態與可預約時段
       -> store.js 建立預約、提出取消
工作臺 -> store.js 設定排班與例外、五種櫃台處置、回診確認
store  -> vendored packages/domain planners + browser adapters
       -> 稽核、outbox 意圖、localStorage
```

正式患者網站要先關閉隱私告知、身分驗證與本人資料存取決策（D-001～D-003、
D-011），並完成已核准 D-006 的 Stage 2 身分／權限實作。本頁只能出現在已記錄、
`noindex`、到期且 synthetic-only 的 Hosting preview；不能把它當成正式掛號入口
或輸入真實資料。

目前的實作允許工作臺以日期、時間與選單輸入調整排班規則，並以實際填寫的患者
資料建立預約與回診。輸入值只存在該瀏覽器的 `localStorage`，按「清除本機資料」
或清除網站資料即消失；它不會改變正式門診表或任何外部日曆。

## 驗收條件

1. 有效週規則可產生可預約時段；重疊、倒置時間與無效星期被拒絕。
2. 指定 `closed` 日期不產生時段；合法 `extra_open` 僅增加指定時段。
3. 時段保留仍透過伺服器交易／冪等規則，不能以 UI 或 Google Calendar 防衝突。
4. 回診只能由明確、可稽核的授權決定開放；患者端不能寫入決定。
5. 患者端只看合成的本人資料與合成時段，沒有個資、醫療內容或外部連線。
6. 重設測試狀態會回復固定合成基準，且不會影響任何外部系統。
7. 鍵盤操作、焦點、錯誤提示和窄螢幕布局都通過既有 UI 檢查。

## 正式上線前必填決策

| 決策 | 所屬核准 |
| --- | --- |
| ✅ 每週營業時間：週三至週五 12:00–20:00、週六 10:00–18:00；更正官網 | D-004 partial, 2026-07-28 |
| ✅ 一筆預約可同時選多項服務；不自動相加服務分鐘，尚待 slot 占用、緩衝與容量算法 | D-004 partial, 2026-07-28 |
| ✅ 止鼾／醫美實際時長到診後形成、不寫死；reservation interval 與 encounter actual timestamps 分開 | D-004 partial, 2026-07-28 |
| 每個 operational slot／資源的區間、緩衝、容量、開放範圍、排班與例外的建立及核准權限 | D-004 |
| 患者取消／改期與回診逾期的處理、通知與人工補救 | D-005 |
| 可設定排班、休診／加開與回診需求的角色，以及患者身分驗證 | D-004／D-007／D-014、D-001～D-003／D-011；staff security baseline D-006 已核准 |
| 回診個案與個案管理師指派、改派、統計規則 | D-007、D-008 |
| 隱私政策、資料保存、患者權利處理與公開網站 URL／無障礙需求 | D-001～D-003、D-011 |

未來 Calendar inbound 必須把同步狀態顯示在正式工作臺；是否一律先進人工 review
queue 仍待 D-016 明確核准。此合成工作臺不得先新增 receiver 或把 browser state
誤當成持久化 candidate store。

每個答案需填入決策登錄的核准者、日期與證據後，才能替換合成值或啟用正式端點。
