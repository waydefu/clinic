import { stagingRequest } from './staging-store-v2.js';
import { renderAccounts,renderAppointments,renderAudit,renderCaseAssignments,renderFollowUps,renderOutbox,renderReleases,renderSchedule,renderSlots,renderTasks,renderWorkload } from './modules/admin-view.js';
import { escapeHtml,roleLabel } from './modules/ui-format.js';
const adminStyles=document.createElement('link');adminStyles.rel='stylesheet';adminStyles.href='/admin-v2.css';document.head.append(adminStyles);
const shell=await fetch('/admin-shell.html').then((response)=>{if(!response.ok)throw new Error('無法載入管理工作臺介面。');return response.text();});document.body.innerHTML=shell;
const elements=Object.fromEntries([...document.querySelectorAll('[id]')].map((element)=>[element.id,element]));
const isOnline=!['127.0.0.1','localhost'].includes(window.location.hostname);let state;let filters={status:'all',patientId:'all'};let selectedPatientId='patient_test_001';
function message(text,tone='info'){elements.status.textContent=text;elements.status.dataset.state=tone;}
async function post(path,body={}){state=await stagingRequest(path,{method:'POST',body:JSON.stringify(body)});render();return state;}
function options(items,selectedId,label){return items.map((item)=>`<option value="${escapeHtml(item.id)}" ${item.id===selectedId?'selected':''}>${escapeHtml(label(item))}</option>`).join('');}
function renderSession(){const active=state.workspace.accounts.filter((item)=>item.status==='active');elements['workspace-account'].innerHTML=options(active,state.session.account.id,(item)=>`${item.label} · ${roleLabel(item.role)}`);elements['current-account-label'].textContent=`${state.session.account.label} · ${roleLabel(state.session.account.role)}`;elements['current-account-boundary'].textContent=state.session.account.role==='admin'?'可設定排班、回診、個管、帳號與系統治理。':'只保留預約、取消與完成到診的日常權限。';const admin=state.session.account.role==='admin';document.querySelectorAll('[data-admin-only],[data-admin-nav]').forEach((element)=>{element.hidden=!admin;});}
function renderFilters(){elements['booking-patient'].innerHTML=options(state.patients,selectedPatientId,(item)=>item.label);elements['appointment-patient-filter'].innerHTML=`<option value="all">全部合成患者</option>${options(state.patients,filters.patientId,(item)=>item.label)}`;elements['appointment-status-filter'].value=filters.status;}
function renderSummary(){elements['available-count'].textContent=String(state.slots.filter((slot)=>slot.reservationId===undefined).length);elements['confirmed-count'].textContent=String(state.appointments.filter((item)=>['confirmed','cancellation_requested'].includes(item.status)).length);elements['completed-count'].textContent=String(state.appointments.filter((item)=>item.status==='completed').length);elements['workload-count'].textContent=String(state.workload.reduce((sum,item)=>sum+item.uniquePatientCount,0));elements['task-list'].innerHTML=renderTasks(state);elements['schedule-version-chip'].textContent=`已發布排班 v${state.scheduleMeta.publishedVersion}`;}
function renderCommunicationForms(){const a=state.workspace.announcement;elements['operations-announcement'].hidden=a.status!=='published';elements['operations-announcement'].innerHTML=a.status==='published'?`<strong>${escapeHtml(a.title)}</strong><span>${escapeHtml(a.body)}</span>`:'';elements['announcement-status'].value=a.status;elements['announcement-title'].value=a.title;elements['announcement-body'].value=a.body;const m=state.workspace.maintenance;elements['maintenance-enabled'].checked=m.enabled;elements['maintenance-title'].value=m.title;elements['maintenance-body'].value=m.body;elements['maintenance-starts-at'].value=m.startsAt??'';elements['maintenance-resume-at'].value=m.resumeAt??'';elements['release-list'].innerHTML=renderReleases(state.workspace);}
function render(){renderSession();renderFilters();renderSummary();elements.slots.innerHTML=renderSlots(state,selectedPatientId);elements.appointments.innerHTML=renderAppointments(state,filters);elements['published-schedule'].innerHTML=renderSchedule(state.schedule,false);elements['draft-schedule'].innerHTML=renderSchedule(state.scheduleDraft,true);elements['schedule-draft-status'].textContent=state.scheduleMeta.draftDirty?'有未發布變更':'草稿與發布版本一致';elements['schedule-draft-status'].className=`status-chip ${state.scheduleMeta.draftDirty?'is-reserved':'is-available'}`;elements['publish-schedule'].disabled=!state.scheduleMeta.draftDirty;elements['discard-schedule'].disabled=!state.scheduleMeta.draftDirty;elements['follow-up-list'].innerHTML=renderFollowUps(state);elements['case-assignment-list'].innerHTML=renderCaseAssignments(state);elements.workload.innerHTML=renderWorkload(state);elements['account-list'].innerHTML=renderAccounts(state.workspace);renderCommunicationForms();elements['audit-events'].innerHTML=renderAudit(state,elements['audit-filter'].value);elements['outbox-jobs'].innerHTML=renderOutbox(state);}
async function updateDraft(mutator,text){const draft=structuredClone(state.scheduleDraft);mutator(draft);await post('/schedule/draft',draft);message(text,'success');}
elements['workspace-account'].addEventListener('change',async()=>{try{await post('/workspace/session',{accountId:elements['workspace-account'].value});message(`已切換為 ${state.session.account.label}。`,'success');}catch(error){message(error.message,'error');}});
elements['reset-state'].addEventListener('click',async()=>{if(!window.confirm('確定重設目前瀏覽器的全部合成資料？此動作不影響任何正式資料。'))return;state=await stagingRequest('/reset',{method:'POST',body:'{}'});filters={status:'all',patientId:'all'};selectedPatientId='patient_test_001';render();message('合成測試資料已重設。','success');});
elements['booking-patient'].addEventListener('change',()=>{selectedPatientId=elements['booking-patient'].value;elements.slots.innerHTML=renderSlots(state,selectedPatientId);});
elements['appointment-status-filter'].addEventListener('change',()=>{filters.status=elements['appointment-status-filter'].value;elements.appointments.innerHTML=renderAppointments(state,filters);});
elements['appointment-patient-filter'].addEventListener('change',()=>{filters.patientId=elements['appointment-patient-filter'].value;elements.appointments.innerHTML=renderAppointments(state,filters);});
elements.slots.addEventListener('click',async(event)=>{const button=event.target.closest('[data-reserve-slot]');if(button===null)return;try{await post('/bookings',{slotId:button.dataset.reserveSlot,patientId:selectedPatientId,bookingType:'initial',origin:'staff'});message('合成預約已建立；可在下方繼續取消或完成到診。','success');}catch(error){message(error.message,'error');}});
elements.appointments.addEventListener('click',async(event)=>{const button=event.target.closest('[data-appointment-action]');if(button===null)return;const action=button.dataset.appointmentAction;const question=action==='complete'?'確認此合成患者已完成到診？':'確認取消此合成預約並釋放時段？';if(!window.confirm(question))return;try{await post(`/bookings/${button.dataset.appointmentId}/${action}`);message(action==='complete'?'完成到診已記錄，請接續處理回診與個管指派。':'合成預約已取消並釋放時段。','success');}catch(error){message(error.message,'error');}});
elements['weekly-form'].addEventListener('submit',async(event)=>{event.preventDefault();const weekdays=[...document.querySelectorAll('.weekday-picker input:checked')].map((item)=>Number(item.value));if(weekdays.length===0)return message('至少選擇一個星期。','error');try{await updateDraft((draft)=>{for(const weekday of weekdays){let entry=draft.weeklyAvailability.find((item)=>item.weekday===weekday);if(entry===undefined){entry={weekday,intervals:[]};draft.weeklyAvailability.push(entry);}entry.intervals.push({startLocalTime:elements['weekly-start'].value,endLocalTime:elements['weekly-end'].value});entry.intervals.sort((a,b)=>a.startLocalTime.localeCompare(b.startLocalTime));}draft.weeklyAvailability.sort((a,b)=>a.weekday-b.weekday);},`已為 ${weekdays.length} 個星期加入排班草稿。`);}catch(error){message(error.message,'error');}});
elements['date-exception-form'].addEventListener('submit',async(event)=>{event.preventDefault();try{await updateDraft((draft)=>{const kind=elements['exception-kind'].value;const entry={date:elements['exception-date'].value,kind,intervals:kind==='extra_open'?[{startLocalTime:elements['exception-start'].value,endLocalTime:elements['exception-end'].value}]:[]};draft.dateExceptions=[...draft.dateExceptions.filter((item)=>item.date!==entry.date),entry].sort((a,b)=>a.date.localeCompare(b.date));},'日期例外已加入排班草稿。');}catch(error){message(error.message,'error');}});
elements['draft-schedule'].addEventListener('click',async(event)=>{const weekly=event.target.closest('[data-remove-weekly]');const exception=event.target.closest('[data-remove-exception]');if(weekly===null&&exception===null)return;if(!window.confirm('確定從排班草稿刪除此設定？'))return;try{await updateDraft((draft)=>{if(weekly!==null){const [weekdayText,indexText]=weekly.dataset.removeWeekly.split(':');const entry=draft.weeklyAvailability.find((item)=>item.weekday===Number(weekdayText));entry.intervals.splice(Number(indexText),1);draft.weeklyAvailability=draft.weeklyAvailability.filter((item)=>item.intervals.length>0);}else draft.dateExceptions=draft.dateExceptions.filter((item)=>item.date!==exception.dataset.removeException);},'排班草稿項目已刪除。');}catch(error){message(error.message,'error');}});
elements['publish-schedule'].addEventListener('click',async()=>{if(!window.confirm('發布後將重新產生患者可預約時段。系統會阻擋影響既有預約的變更，是否繼續？'))return;try{await post('/schedule/publish');message('排班已發布，病患端可預約時段已同步更新。','success');}catch(error){message(error.message,'error');}});
elements['discard-schedule'].addEventListener('click',async()=>{if(!window.confirm('確定捨棄所有未發布的排班變更？'))return;await post('/schedule/discard');message('未發布排班變更已捨棄。','success');});
elements['follow-up-list'].addEventListener('submit',async(event)=>{const form=event.target.closest('[data-follow-up-form]');if(form===null)return;event.preventDefault();const data=new FormData(form);try{await post(`/follow-ups/${form.dataset.followUpForm}`,{status:data.get('status'),dueDate:data.get('dueDate')});message('逐筆回診決定已記錄。','success');}catch(error){message(error.message,'error');}});
elements['case-assignment-list'].addEventListener('submit',async(event)=>{const form=event.target.closest('[data-case-form]');if(form===null)return;event.preventDefault();const data=new FormData(form);try{await post('/case-assignments',{appointmentId:form.dataset.caseForm,managerId:data.get('managerId')});message('個管指派已更新，月度不重複患者統計已重新計算。','success');}catch(error){message(error.message,'error');}});
elements['account-form'].addEventListener('submit',async(event)=>{event.preventDefault();try{await post('/workspace/accounts',{label:elements['account-label'].value,role:elements['account-role'].value});message('合成帳號已建立。','success');}catch(error){message(error.message,'error');}});
elements['account-list'].addEventListener('click',async(event)=>{const button=event.target.closest('[data-account-toggle]');if(button===null)return;if(!window.confirm('確定變更此合成帳號的啟用狀態？'))return;try{await post(`/workspace/accounts/${button.dataset.accountToggle}/toggle`);message('合成帳號狀態已更新。','success');}catch(error){message(error.message,'error');}});
elements['announcement-form'].addEventListener('submit',async(event)=>{event.preventDefault();try{await post('/workspace/announcement',{status:elements['announcement-status'].value,title:elements['announcement-title'].value,body:elements['announcement-body'].value});message('患者公告已儲存。','success');}catch(error){message(error.message,'error');}});
elements['maintenance-form'].addEventListener('submit',async(event)=>{event.preventDefault();if(elements['maintenance-enabled'].checked&&!window.confirm('啟用後，生效時間內患者端將無法建立預約。確定套用？'))return;try{await post('/workspace/maintenance',{enabled:elements['maintenance-enabled'].checked,title:elements['maintenance-title'].value,body:elements['maintenance-body'].value,startsAt:elements['maintenance-starts-at'].value,resumeAt:elements['maintenance-resume-at'].value});message(state.maintenanceActive?'患者端維護模式目前生效中。':'維護排程已儲存，目前尚未生效或已自動恢復。','success');}catch(error){message(error.message,'error');}});
elements['release-form'].addEventListener('submit',async(event)=>{event.preventDefault();try{await post('/workspace/releases',{version:elements['release-version'].value,summary:elements['release-summary'].value});message('合成發布紀錄已新增；未觸發真實部署。','success');}catch(error){message(error.message,'error');}});
elements['audit-filter'].addEventListener('change',()=>{elements['audit-events'].innerHTML=renderAudit(state,elements['audit-filter'].value);});
document.querySelector('.skip-link').addEventListener('click',(event)=>{event.preventDefault();window.location.hash='main-content';elements['main-content'].focus({preventScroll:true});});
if(!isOnline)elements['environment-label'].textContent='LOCAL TEST ONLY';
try{state=await stagingRequest('/state');render();message('管理工作臺已就緒。所有內容皆為合成資料。','success');}catch(error){message(error instanceof Error?error.message:'無法載入合成工作臺。','error');}
