import { ROLE_LABELS } from './constants.js';
import { currentAccount } from './permissions.js';
export function safeText(value,label,maximum){if(typeof value!=='string'||value.trim().length===0||value.trim().length>maximum)throw new Error(`${label}必須為 1–${maximum} 個字元。`);return value.trim();}
export function switchAccount(state,accountId){const account=state.workspace.accounts.find((item)=>item.id===accountId&&item.status==='active');if(account===undefined)throw new Error('找不到可登入的合成帳號。');state.workspace.currentAccountId=account.id;}
export function createAccount(state,input){if(!Object.hasOwn(ROLE_LABELS,input.role))throw new Error('合成角色無效。');const suffix=String(state.workspace.accountSequence).padStart(3,'0');const prefix=input.role==='admin'?'admin':'front_desk';state.workspace.accounts.push({id:`${prefix}_test_${suffix}`,label:safeText(input.label,'帳號標籤',24),role:input.role,status:'active'});state.workspace.accountSequence+=1;}
export function toggleAccount(state,accountId){const account=state.workspace.accounts.find((item)=>item.id===accountId);if(account===undefined)throw new Error('找不到合成帳號。');if(account.id===state.workspace.currentAccountId&&account.status==='active')throw new Error('不可停用目前正在操作的合成帳號。');if(account.role==='admin'&&account.status==='active'){const active=state.workspace.accounts.filter((item)=>item.role==='admin'&&item.status==='active');if(active.length<=1)throw new Error('至少必須保留一個啟用中的合成管理者。');}account.status=account.status==='active'?'disabled':'active';}
export function saveAnnouncement(state,input){state.workspace.announcement={status:input.status==='draft'?'draft':'published',title:safeText(input.title,'公告標題',40),body:safeText(input.body,'公告內容',160),updatedAt:new Date().toISOString(),updatedBy:currentAccount(state)?.id};}
export function saveMaintenance(state,input){state.workspace.maintenance={enabled:input.enabled===true,title:safeText(input.title,'維護標題',40),body:safeText(input.body,'維護說明',160),startsAt:typeof input.startsAt==='string'?input.startsAt:'',resumeAt:typeof input.resumeAt==='string'?input.resumeAt:'',updatedAt:new Date().toISOString(),updatedBy:currentAccount(state)?.id};}
export function isMaintenanceActive(maintenance,now=new Date()){if(maintenance.enabled!==true)return false;const starts=maintenance.startsAt?new Date(`${maintenance.startsAt}:00+08:00`):undefined;const resume=maintenance.resumeAt?new Date(`${maintenance.resumeAt}:00+08:00`):undefined;if(starts!==undefined&&now<starts)return false;if(resume!==undefined&&now>=resume)return false;return true;}
export function addRelease(state,input){const nextId=`release_test_${String(state.workspace.releases.length+1).padStart(3,'0')}`;state.workspace.releases.unshift({id:nextId,version:safeText(input.version,'版本',24),summary:safeText(input.summary,'發布摘要',120),publishedAt:new Date().toISOString(),publishedBy:currentAccount(state)?.id});}
