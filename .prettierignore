node_modules/
dist/
coverage/
.firebase/
output/
pnpm-lock.yaml

# 編譯產物：packages/domain 建置後同步過來的，不由本專案排版。
apps/web/public/vendor/

# Claude Code 的巢狀 worktree。它是同一個 repo 的另一個 checkout，剛好放在工作區
# 內部，裡面有它自己的 dist／vendor 建置產物。
#
# 為什麼需要這一條：prettier 不讀 .gitignore，只讀這個檔案，所以 git 看不見的巢狀
# checkout 對它是可見的。而上面那條 `apps/web/public/vendor/` 含斜線、錨定在根目錄，
# match 不到 `.claude/worktrees/<name>/apps/web/public/vendor/`。結果是**只要有人開
# 一個 worktree，父目錄的 `check:format` 就會因為與自己改動無關的檔案變紅**——
# 2026-08-02 實際發生過一次，16 個違規全部來自另一個 session 的 checkout。
#
# 只排除 worktrees/，不是整個 .claude/：`launch.json` 等設定仍然要被檢查。
.claude/worktrees/

# Prose and hand-aligned governance tables are authored by reviewers, not by a
# formatter. Keeping them out avoids noisy diffs across ADRs and approval packets.
*.md
