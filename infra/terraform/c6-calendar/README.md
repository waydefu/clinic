# C6 Calendar API (source only)

**Not C6 authority and not production Calendar.** Default SHA creates
zero resources. Enables `calendar-json.googleapis.com` only. Rejects
`beauessence-clinic-staging`. Does not mount `/v1/bookings` or
`CalendarWatchController`. Watch helpers remain unwired until C6 apply
after C1～C5 PASS. Copy `terraform.tfvars.example` locally. Local packet:
[c2-c6-local-execution-packet.md](../../../docs/runbooks/c2-c6-local-execution-packet.md).
